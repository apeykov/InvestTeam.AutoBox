namespace InvestTeam.AutoBox.Domain.Enums
{
    /// <summary>
    /// Type of the object into the app domain & for the history table.
    ///     1) Used for logging purposes;
    /// </summary>
    public enum ObjectType
    {
        User = 0,
        Order = 1,
        Vechicle = 2
    }
}
